# ===========================================
# CONFIGURAÇÃO DO SISTEMA DE AGENDAMENTOS
# ===========================================

# Configuração do Servidor (OBRIGATÓRIO)
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Configuração do Banco de Dados (OBRIGATÓRIO)
# PostgreSQL - Configuração obrigatória
DB_HOST=localhost
DB_PORT=5433
DB_NAME=agendamento
DB_USER=agendamento_user
DB_PASSWORD=agendamento_pass_2024

# Configuração Avançada do Banco (OPCIONAL)
# DB_SSL=false
# DB_MAX_CONNECTIONS=20
# DB_MIN_CONNECTIONS=2
# DB_IDLE_TIMEOUT=30000
# DB_CONNECTION_TIMEOUT=10000

# Configuração de Segurança (OBRIGATÓRIO)
# IMPORTANTE: Altere estes valores em produção!
JWT_SECRET=troque-para-um-segredo-forte-com-pelo-menos-32-caracteres
JWT_EXPIRES_IN=24h
SESSION_SECRET=sessao-secreta-padrao

# Configuração de Rede (OPCIONAL)
# Permitir origens específicas (separadas por vírgula)
# ALLOWED_ORIGINS=http://192.168.1.100:3000,http://10.0.0.50:3000

# Configuração do WhatsApp (OPCIONAL)
# WHATSAPP_SESSION_PATH=./data/whatsapp-auth
# WHATSAPP_WEBHOOK_URL=http://seu_ip:3000/api/whatsapp/webhook

# ===========================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ===========================================
# 1. Copie este arquivo para .env
# 2. Altere as credenciais do banco de dados
# 3. Defina um JWT_SECRET forte (mínimo 32 caracteres)
# 4. Configure as URLs permitidas se necessário
# 5. Reinicie o servidor após as alterações
